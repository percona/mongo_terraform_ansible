---
- name: Install MongoDB
  hosts: cfg:shards:replsets
  gather_facts: "{{ not (skip_repeated_facts | default(false) | bool) }}"
  become: yes
  tags: install
  environment:
    PERCONA_TELEMETRY_DISABLE: "{{ '1' if disable_telemetry else '0' }}"
  tasks:
    - name: Remove conflicting Percona mongosh before installing MongoDB Enterprise
      command: dpkg --remove --force-depends percona-mongodb-mongosh
      register: remove_percona_mongosh
      changed_when: remove_percona_mongosh.rc == 0
      failed_when: >-
        remove_percona_mongosh.rc != 0 and
        'is not installed' not in remove_percona_mongosh.stderr
      when:
        - mongodb_distribution_effective == 'enterprise'
        - ansible_facts['os_family'] == 'Debian'

    - name: Repair package dependencies before installing MongoDB Enterprise
      apt:
        state: fixed
      when:
        - mongodb_distribution_effective == 'enterprise'
        - ansible_facts['os_family'] == 'Debian'

    - name: Remove MongoDB mongosh before installing Percona Server for MongoDB
      command: dpkg --remove --force-depends mongodb-mongosh
      register: remove_mongodb_mongosh
      changed_when: remove_mongodb_mongosh.rc == 0
      failed_when: >-
        remove_mongodb_mongosh.rc != 0 and
        'is not installed' not in remove_mongodb_mongosh.stderr
      when:
        - mongodb_distribution_effective == 'psmdb'
        - ansible_facts['os_family'] == 'Debian'

    - name: Remove MongoDB Enterprise packages before installing Percona Server for MongoDB
      shell: >-
        for package in mongodb-enterprise mongodb-enterprise-database
        mongodb-enterprise-server mongodb-enterprise-mongos mongodb-enterprise-tools
        mongodb-enterprise-shell mongodb-enterprise-cryptd
        mongodb-enterprise-database-tools-extra mongodb-database-tools; do
        dpkg --purge --force-depends "$package" 2>/dev/null || true;
        done
      changed_when: false
      when:
        - mongodb_distribution_effective == 'psmdb'
        - ansible_facts['os_family'] == 'Debian'

    - name: Repair package dependencies before installing Percona Server for MongoDB
      apt:
        state: fixed
      when:
        - mongodb_distribution_effective == 'psmdb'
        - ansible_facts['os_family'] == 'Debian'

    - name: Install mongod packages from repository (latest)
      package:
        name: "{{ mongod_packages }}"
        state: present
      when: mongo_version is not defined or mongo_version == ''

    - name: Install mongod packages from repository (specific version – Debian/Ubuntu)
      apt:
        name: "{{ mongod_versioned_packages | map('regex_replace', '^(percona-server-mongodb|mongodb-org|mongodb-org-database|mongodb-org-server|mongodb-org-mongos|mongodb-org-tools|mongodb-enterprise|mongodb-enterprise-database|mongodb-enterprise-server|mongodb-enterprise-mongos|mongodb-enterprise-tools)$', '\\1=' ~ mongo_version ~ '*') | list }}"
        state: present
        allow_downgrade: true
      when:
        - mongo_version is defined and mongo_version != ''
        - ansible_facts['os_family'] == 'Debian'

    - name: Install mongod packages from repository (specific version – RedHat)
      yum:
        name: "{{ mongod_versioned_packages | map('regex_replace', '^(percona-server-mongodb|mongodb-org|mongodb-org-database|mongodb-org-server|mongodb-org-mongos|mongodb-org-tools|mongodb-enterprise|mongodb-enterprise-database|mongodb-enterprise-server|mongodb-enterprise-mongos|mongodb-enterprise-tools)$', '\\1-' ~ mongo_version) | list }}"
        state: present
      when:
        - mongo_version is defined and mongo_version != ''
        - ansible_facts['os_family'] == 'RedHat'

    - name: Disable Percona telemetry
      systemd:
        name: percona-telemetry-agent
        enabled: false
        state: stopped
        daemon_reload: yes
      ignore_errors: true
      when:
        - disable_telemetry | bool
        - mongodb_distribution_effective == 'psmdb'

- name: Prepare the configuration
  hosts: cfg:shards:replsets
  gather_facts: "{{ not (skip_repeated_facts | default(false) | bool) }}"
  tags: bootstrap_rs
  
  vars:
    mongo_tls_flags: >-
      {% if use_tls %}
      --tls
      --tlsCertificateKeyFile {{ certificateKeyFile }}
      --tlsCAFile {{ CAFile }}
      --tlsAllowInvalidCertificates
      {% endif %}

  tasks:
    - name: Set up shared mongo keyfile 
      become: yes
      copy:
        dest: "{{ keyFile_location }}"
        content: "{{ keyfile_content }}"
        owner: "{{ mongo_os_user }}"
        group: root
        mode: 0600
      when: not use_tls | bool
      notify: restart mongod

    - name: Copy mongod.conf 
      become: yes
      template:
        src: "{{ mongod_conf_template }}"
        dest: /etc/mongod.conf
        owner: root
        group: root
        mode: 0644
      notify: restart mongod

    - name: Create the MongoDB directories
      become: yes
      file:
        path: "{{ item }}"
        owner: "{{ mongo_os_user }}"
        group: "{{ mongo_os_group }}"
        mode: '0750'
        state: directory
        recurse: yes
      with_items:
        - "{{ mongod_path }}"
        - "{{ mongod_log_path }}"

    - name: Ensure MongoDB log file ownership
      become: yes
      file:
        path: "{{ mongod_log_path }}/mongod.log"
        state: touch
        owner: "{{ mongo_os_user }}"
        group: "{{ mongo_os_group }}"
        mode: '0640'

    - name: Ensure audit log directory exists
      become: yes
      file:
        path: "{{ audit_log_path | dirname }}"
        owner: "{{ mongo_os_user }}"
        group: "{{ mongo_os_group }}"
        mode: '0750'
        state: directory
      when: enable_audit | bool

    - name: Set up keyfile for encryption at rest
      become: yes
      copy:
        dest: "{{ encryption_keyfile }}"
        content: "{{ encryption_key_content }}"
        owner: "{{ mongo_os_user }}"
        group: root
        mode: 0600
      when: keyfile_encryption | bool
      notify: restart mongod

    - name: Copy Vault crt and mongo token file to all servers
      block:

        - name: Create directory for vault credentials
          become: yes
          file:
            path: /etc/mongodb
            state: directory
            owner: "{{ mongo_os_user }}"
            group: "{{ mongo_os_group }}"
            mode: 0500

        - name: Fetch the Vault crt file from Vault server to control machine
          become: yes
          fetch:
            src: /etc/vault.d/vault.crt
            dest: ./vault.crt
            flat: yes
          delegate_to: "{{ (groups['pmm'] | default([])) | first | default(inventory_hostname) }}"
          run_once: true

        - name: Copy vault certificate to MongoDB hosts
          become: yes
          copy:
            src: "{{ playbook_dir }}/vault.crt"
            dest: "{{ vault_cafile }}"
            owner: "{{ mongo_os_user }}"
            group: "{{ mongo_os_group }}"
            mode: '0400'
          notify: restart mongod

        - name: Fetch the mongo token file from Vault server to control machine
          become: yes
          fetch:
            src: /tmp/mongo_token.txt
            dest: ./mongo_token.txt
            flat: yes
          delegate_to: "{{ (groups['pmm'] | default([])) | first | default(inventory_hostname) }}"
          run_once: true

        - name: Copy token file to MongoDB hosts
          become: yes
          copy:
            src: "{{ playbook_dir }}/mongo_token.txt"
            dest: "{{ vault_tokenfile }}"
            owner: "{{ mongo_os_user }}"
            group: "{{ mongo_os_group }}"
            mode: '0400'
          notify: restart mongod

      when: vault_encryption | bool

    - name: Ensure mongod is started
      become: yes
      service:
        name: mongod
        state: started
        enabled: yes

    - name: Apply pending mongod restarts
      meta: flush_handlers

    - name: Prepare the init command for replica set
      template:
        src: "{{ rs_init_template }}"
        dest: /tmp/init-rs.js
        mode: 0644
      when: mongodb_primary | default(false)

    - name: Wait for MongoDB port
      wait_for:
        host: 127.0.0.1
        port: "{{ mongo_port }}"
        delay: 1
        timeout: 60

    - name: Initialize replica set
      shell: >
        mongosh {{ mongo_tls_flags }}
        --port {{ mongo_port }}
        --host 127.0.0.1
        --quiet
        --eval 'db.hello().setName || ""'
      register: replica_set_name
      changed_when: false
      when: mongodb_primary | default(false)

    - name: Initialize an uninitialized replica set
      command: >
        mongosh {{ mongo_tls_flags }}
        --port {{ mongo_port }}
        --host 127.0.0.1
        /tmp/init-rs.js
      when:
        - mongodb_primary | default(false)
        - replica_set_name.stdout | trim == ''

    - name: Wait for PRIMARY
      shell: >
        mongosh {{ mongo_tls_flags }}
        --port {{ mongo_port }}
        --host 127.0.0.1
        --quiet
        --eval 'db.hello().isWritablePrimary'
        | grep -q true
      register: primary_check
      retries: 30
      delay: 1
      until: primary_check.rc == 0
      changed_when: false
      when: mongodb_primary | default(false)

  handlers:
    - name: restart mongod
      become: yes
      service:
        name: mongod
        state: restarted

- name: Create the local users & logrotate
  hosts: cfg:shards:replsets
  gather_facts: "{{ not (skip_repeated_facts | default(false) | bool) }}"
  become: yes
  tags: os_conf

  vars:
    mongo_tls_flags: >-
      {% if use_tls %}
      --tls
      --tlsCertificateKeyFile {{ certificateKeyFile }}
      --tlsCAFile {{ CAFile }}
      --tlsAllowInvalidCertificates
      {% endif %}

  tasks:
    - name: Copy logrotate conf
      template:
        src: templates/logrotate-mongod.conf.j2
        dest: /etc/logrotate.d/mongod
        mode: 0644

    - name: Create MongoDB root user
      shell: >
        mongosh admin {{ mongo_tls_flags }}
        --port {{ mongo_port }}
        --host 127.0.0.1
        --eval 'db.createUser({user:"{{ mongo_admin_user }}",pwd:"{{ mongo_admin_password }}",roles:["root"]})'
      no_log: true
      register: root_user_result
      changed_when: root_user_result.rc == 0
      failed_when: root_user_result.rc != 0 and 'requires authentication' not in root_user_result.stderr
      when: mongodb_primary | default(false)

    - name: Prepare the commands to create the PBM and PMM users and roles
      template:
        src: templates/createUsers.js.j2
        dest: /tmp/createUsers.js
        mode: 0644
      when: mongodb_primary | default(false)

    - name: Create PBM and PMM users
      shell: >
        mongosh admin {{ mongo_tls_flags }}
        -u {{ mongo_admin_user }}
        -p {{ mongo_admin_password }}
        --port {{ mongo_port }}
        --host {{ ansible_facts['fqdn'] }}
        < /tmp/createUsers.js
      no_log: true
      when: mongodb_primary | default(false)

    - name: Prepare LDAP external users
      template:
        src: templates/createLdapUsers.js.j2
        dest: /tmp/createLdapUsers.js
        mode: 0600
      when:
        - mongodb_primary | default(false)
        - enable_ldap | default(false) | bool
        - ldap_mongodb_users | default([]) | length > 0

    - name: Create LDAP external users
      shell: >
        mongosh admin {{ mongo_tls_flags }}
        -u {{ mongo_admin_user }}
        -p {{ mongo_admin_password }}
        --port {{ mongo_port }}
        --host {{ ansible_facts['fqdn'] }}
        < /tmp/createLdapUsers.js
      no_log: true
      when:
        - mongodb_primary | default(false)
        - enable_ldap | default(false) | bool
        - ldap_mongodb_users | default([]) | length > 0

    - name: Configure mongo shell alias
      lineinfile:
        path: /etc/profile
        line: >
          alias mongo='mongosh admin {{ mongo_tls_flags }}
          -u {{ mongo_admin_user }}
          -p {{ mongo_admin_password }}
          --port {{ mongo_port }}
          --host {{ ansible_facts['fqdn'] }}'
        state: present
