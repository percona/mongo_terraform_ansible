---
- name: Gather facts from the PMM server
  hosts: pmm
  gather_facts: yes
  tags: monitoring
  tasks:
    - name: Dummy task to force fact gathering
      debug:
        msg: "Facts collected for PMM server"

- name: Install PMM client
  hosts: cfg:shards:replsets:mongos
  become: yes
  tags: install,monitoring
  environment:
    PERCONA_TELEMETRY_DISABLE: "{{ '1' if disable_telemetry else '0' }}"
  tasks:
    - name: Enable PMM client repository
      shell: "/usr/bin/percona-release enable {{ pmm_client_repo }}"

    - name: Install pmm-client package from repository (latest)
      package:
        name: "{{ pmm_client_package }}"
        state: present
      when: pmm_client_version is not defined or pmm_client_version == ''

    - name: Install pmm-client packages from repository (specific version – Debian/Ubuntu)
      apt:
        name: "{{ pmm_client_package | map('regex_replace', '^pmm-client$', 'pmm-client=' ~ pmm_client_version ~ '*') | list }}"
        state: present
        allow_downgrade: true
      when:
        - pmm_client_version is defined and pmm_client_version != ''
        - ansible_facts['os_family'] == 'Debian'

    - name: Install pmm-client packages from repository (specific version – RedHat)
      yum:
        name: "{{ pmm_client_package | map('regex_replace', '^pmm-client$', 'pmm-client-' ~ pmm_client_version) | list }}"
        state: present
      when:
        - pmm_client_version is defined and pmm_client_version != ''
        - ansible_facts['os_family'] == 'RedHat'

    - name: Disable Percona telemetry
      systemd:
        name: percona-telemetry-agent
        enabled: false
        state: stopped
        daemon_reload: yes
      ignore_errors: true
      when: disable_telemetry | bool

- name: Configure monitoring with PMM
  hosts: cfg:shards:replsets:mongos
  become: true
  tags: monitoring
  tasks:
    - name: Ensure pmm-agent is started
      service:
        name: pmm-agent
        state: started

    - name: Point pmm-agent to the PMM server
      shell: pmm-admin config --server-url=https://{{ pmm_server_user }}:{{ pmm_server_pwd }}@{{ hostvars[groups['pmm'][0]].ansible_facts['fqdn'] }}:{{ pmm_external_port }} --server-insecure-tls --force
      register: pmm_config_result
      retries: 3
      delay: 1
      until: pmm_config_result.rc == 0      

    - name: Add MongoDB metrics collector
      shell: >
        pmm-admin add mongodb
        --environment={{ env_tag }}
        {% if cluster is defined %}
        --cluster {{ cluster }}
        {% endif %}
        --host={{ ansible_facts['fqdn'] }}
        --port={{ mongo_port }}
        --tls-skip-verify
        {% if arbiter is not defined %}
        --username={{ mongodb_pmm_user }} --password='{{ mongodb_pmm_user_pwd }}'
        {% if use_tls | bool %}
        --tls --tls-certificate-key-file={{ certificateKeyFile }} --tls-ca-file={{ CAFile }}
        {% endif %}
        --enable-all-collectors
        {% endif %}
