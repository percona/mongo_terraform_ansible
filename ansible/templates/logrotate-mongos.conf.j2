{{ mongod_log_path }}/mongos.log {
  daily
  size 100M
  rotate 10
  missingok
  compress
  delaycompress
  notifempty
  create 640 mongod mongod
  sharedscripts
  postrotate
    /bin/kill -SIGUSR1 `cat /var/run/mongos.pid 2>/dev/null` >/dev/null 2>&1
  endscript
}

