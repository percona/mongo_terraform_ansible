[replsets:children]
${rs_name}

[${rs_name}]
%{ for index, group in ansible_group_replsets ~}
${hostname_replsets[index]} ${ index == 0 ? "mongodb_primary=True" : "" }
%{ endfor ~}
%{ for arb_index, arb_group in ansible_group_arbiters ~}
${ hostname_arbiters[arb_index] } arbiter=True
%{ endfor ~}

[pmm]
${hostname_pmm} 

[all:vars]
ansible_ssh_user=${my_ssh_user}
bucket=${bucket}
region=${region}
endpointUrl=${endpointUrl}
access_key_id=${access_key}
secret_access_key=${secret_access_key}
env_tag=${env_tag}
